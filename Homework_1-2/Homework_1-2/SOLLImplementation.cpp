#include "SOLLADT.h"
#include "SOLLNode.h"
#include <string>

using namespace std;

template<class T>
SOLLADT<T>::SOLLADT() : head(NULL)
{
};

SOLLADT<T>::~SOLLADT()
{
};

SOLLADT<T>::search(T element)
{
	SOLLNode::Node<T> current;
	while (current::link != null)
	{

	};
};
	
SOLLADT<T>::search_mtf(T element)
{
};

SOLLADT<T>::search_t(T element)
{
};

SOLLADT<T>::insert(T element)
{
};

SOLLADT<T>::showSOLL()
{
};
